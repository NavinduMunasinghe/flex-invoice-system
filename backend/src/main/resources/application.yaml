spring:
  datasource:
    url: jdbc:mysql://localhost:3306/flex_invoice_db
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080